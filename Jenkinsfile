pipeline {
    agent any
    stages {
        stage('Compile') {
            steps {
                sh 'npm --version'
            }
        }
        stage('Test') {
            steps {
                sh 'npm --version'
            }
        }
        stage('Build') {
            steps {
                sh 'npm --version'
            }
        }
    }
}